/**
 * cold-start benchmark — skeleton.
 *
 * Measures Lambda cold-start latency added by each candidate plugin,
 * against a fixed handler corpus, using AWS X-Ray `Init Duration`.
 *
 * Not yet implemented. See README.md and methodology.md for scope.
 */

console.error('cold-start: not yet implemented');
process.exit(1);
