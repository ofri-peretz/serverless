/**
 * deploy-latency benchmark — skeleton.
 *
 * Measures end-to-end `serverless deploy` wall-clock time with each
 * candidate plugin enabled, against a fixed `serverless.yml` corpus.
 *
 * Not yet implemented. See README.md and methodology.md for scope.
 */

console.error('deploy-latency: not yet implemented');
process.exit(1);
